# libsdl3-sunxifb
SDL3 mainline + sunxifb backend (forward-port of vendor mali-fbdev patch from SDL2-2.26.1.GE8300). Produces libSDL3-pocketforge.so.0 — the ABI artifact loaded by Steam Link via SDL3_DYNAMIC_API.
